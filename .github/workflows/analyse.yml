on:
  pull_request:
    paths-ignore:
      - 'docs/**'
      - 'README.md'
      - 'CHANGELOG.md'
      - '.gitignore'
      - '.gitattributes'

  push:
    paths-ignore:
      - 'docs/**'
      - 'README.md'
      - 'CHANGELOG.md'
      - '.gitignore'
      - '.gitattributes'

name: static analysis

jobs:
  psalm:
    uses: zorachka/actions/.github/workflows/phpstan.yml@main
    with:
      os: >-
        ['ubuntu-latest']
      php: >-
        ['8.0', '8.1']
